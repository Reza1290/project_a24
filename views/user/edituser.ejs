<%- include('../partials/header__') %>
<%- include('../partials/sidebar__') %>

<style>

/* Styling untuk tombol Lihat Data di sebelah kanan */
        .btn-minimal {
            padding: 8px 16px; /* Ukuran kecil tapi tetap nyaman */
            font-size: 14px;   /* Font yang proporsional */
            font-weight: 500;  /* Sedikit bold */
            border: none;      /* Tanpa border untuk kesan modern */
            border-radius: 5px; /* Sudut tumpul ringan */
            cursor: pointer;
            transition: all 0.3s ease; /* Animasi halus */
        }

        .x_title {
            display: flex;
            justify-content: space-between; /* Membuat ruang antara elemen kiri dan kanan */
            align-items: center; /* Vertikal align agar elemen rata tengah */
        }

        .btn-wrapper {
            margin-left: auto; /* Memastikan tombol berada di sebelah kanan */
        }

        .btn-wrapper-lihatdata {
            position: absolute;
            right: 30px; /* Tombol akan terletak di ujung kanan */
            top: 16%; /* Menempatkan tombol di tengah vertikal */
            transform: translateY(-50%); /* Untuk menyeimbangkan posisi tengah vertikal */
        }


        /* Menjaga form-group agar tetap responsif pada layar kecil */
        @media (max-width: 768px) {
            .form-group.double-column {
                flex-direction: column;
            }

            .form-group.double-column .col-md-6 {
                width: 100%;
            }
        }



/* Styling untuk form */
        @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@100;200;300;400;500;600;700&display=swap');

        *{
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        .container{
            border-radius: 0.5rem;
            box-shadow: 0px 0px 0px 1px rgba(0, 0, 0, 0.1),
                        0px 5px 12px -2px rgba(0, 0, 0, 0.1),
                        0px 18px 36px -6px rgba(0, 0, 0, 0.1);
            overflow: hidden;
        }

        .container .title{
            padding-top: 25px;
            padding-left: 25px;
            padding-bottom: 10px;
            background: #f6f8fa;
        }

        .container .title p{
            font-size: 25px;
            font-weight: 500;
            position: relative;
        }

        .container .title p::before{
            content: "";
            position: absolute;
            bottom: 0;
            left: 0;
            width: 30px;
            height: 3px;
            background: linear-gradient(to right, #F37A65, #D64141);
        }

        .user_details{
            display: flex;
            justify-content: space-between;
            flex-wrap: wrap;
            gap: 20px;
            padding: 25px;
        }

        .user_details .input_box{
            width: calc(100% / 2 - 20px);
            margin: 0 0 12px 0;
        }

        .input_box label{
            font-weight: 500;
            margin-bottom: 5px;
            display: block;
        }

        .input_box label::after{
            content: " *";
            color: red;
        }

        .input_box input{
            width: 100%;
            height: 45px;
            border: none;
            outline: none;
            border-radius: 5px;
            font-size: 16px;
            padding-left: 15px;
            padding-right: 15px;
            box-shadow: 0px 0px 0px 1px rgba(0, 0, 0, 0.1);
            background-color: #f6f8fa;
            font-family: 'Poppins', sans-serif;
            transition: all 120ms ease-out 0s;
        }


        .input_box input:focus,
        .input_box input:valid{
            box-shadow: 0px 0px 0px 2px #AC8ECE;
        }

        .reg_btn{
            padding: 25px;
            margin: 15px 0;
            margin-top: -30px;
        }

        .reg_btn input{
            height: 45px;
            width: 100%;
            border: none;
            font-size: 18px;
            font-weight: 500;
            cursor: pointer;
            background: linear-gradient(to right, #F37A65, #D64141);
            border-radius: 5px;
            color: #ffffff;
            letter-spacing: 1px;
            text-shadow: 0px 2px 2px rgba(0, 0, 0, 0.2);
        }

        .reg_btn input:hover{
            background: linear-gradient(to right, #D64141, #F37A65);
        }

        .input_box select{
            width: 100%;
            height: 45px;
            border: none;
            outline: none;
            border-radius: 5px;
            font-size: 16px;
            padding-left: 15px;
            box-shadow: 0px 0px 0px 1px rgba(0, 0, 0, 0.1);
            background-color: #f6f8fa;
            font-family: 'Poppins', sans-serif;
            transition: all 120ms ease-out 0s;
        }


        .input_box input:focus,
        .input_box input:valid{
            box-shadow: 0px 0px 0px 2px #AC8ECE;
        }


        @media screen and (max-width: 584px){

            .user_details{
                max-height: 340px;
                overflow-y: scroll;
            }

            .user_details::-webkit-scrollbar{
                width: 0;
            }

            .user_details .input_box{
                width: 100%;
            }

            .gender .category{
                width: 100%;
            }

        }


        @media screen and (max-width: 419px){
            .gender .category{
                flex-direction: column;
            }   
        }
</style>

<!-- page content -->
<div class="right_col" role="main">
    <div class="page-title">
        <div class="title_left">
            <h3>Manajemen Data Kepala Bidang</h3>
        </div>
        <div class="btn-wrapper-lihatdata">
            <button class="btn-minimal btn-info" onclick="window.location.href='/user/hasilinput'">
                <i class="fa fa-folder-open"></i> Lihat Data
            </button>
        </div>
    </div>

    <div class="clearfix"></div>
    <div class="col-md-12 col-sm-12">
        <div class="x_panel">
            <div class="x_title">
                <h2>Input Kabid <small>Masukkan data masing-masing dari kabid</small></h2>
                <ul class="nav navbar-right panel_toolbox">
                    <li><a class="collapse-link"><i class="fa fa-chevron-up"></i></a></li>
                </ul>
                <div class="clearfix"></div>
            </div>
            <div class="x_content">

                <!-- Smart Wizard -->
                <p style="color: red;">*Pastikan semua data yang Anda masukkan sudah benar dan lengkap sebelum menyimpannya</p>
                <div  class="container">

                    <!-- Step 1 -->
                    <div id="step-1">
                      <div class="container">
                        <div class="title">
                            <p>Registration</p>
                        </div>
                
                        <form action="/user/edit/<%= user.id_user %>" method="POST" enctype="multipart/form-data" id="form-step-1" class="form-horizontal form-label-left">
                            <div class="user_details">
                                <div class="input_box">
                                    <label for="nama_user">Nama Lengkap</label>
                                    <input type="text" id="nama_user" value="<%= user.nama_user %>" name="nama_user" placeholder="Tulis nama lengkap anda" autocomplete="off" required >
                                </div>
                                <div class="input_box">
                                    <label for="nip_user">Nomor NIP</label>
                                    <input type="number" id="nip_user" value="<%= user.nip_user %>" name="nip_user" placeholder="Tulis nomor NIP anda" autocomplete="off" required>
                                </div>
                                <div class="input_box">
                                  <label for="jabatan_user">Jabatan</label>
                                  <input type="text" id="jabatan_user" value="<%= user.jabatan_user %>" name="jabatan_user" placeholder="Tulis jabatan anda" autocomplete="off" required>
                                 </div>
                                 <div class="input_box">
                                  <label for="alamat_user">Alamat</label>
                                  <input type="text" id="alamat_user" value="<%= user.alamat_user %>" name="alamat_user" placeholder="Tulis alamat lengkap anda" autocomplete="off" required>
                                 </div>
                                 <div class="input_box">
                                  <label for="phone">Nomor Telepon</label>
                                  <input type="number" id="no_telp_user" value="<%= user.no_telp_user %>" name="no_telp_user" placeholder="Tulis nomor telepon anda" autocomplete="off" required>
                                 </div>
                                <div class="input_box">
                                    <label for="email_user">Email</label>
                                    <input type="email" id="email_user" value="<%= user.email_user %>" name="email_user" placeholder="Tulis alamat e-mail anda" autocomplete="off" required>
                                </div>
                                <div class="input_box">
                                  <label for="username_user">Username</label>
                                  <input type="text" id="username_user" value="<%= user.username_user %>" name="username_user" placeholder="Buat username baru anda" autocomplete="off" required>
                                </div>
                                <div class="input_box">
                                    <label for="password_user">Password</label>
                                    <input type="password" id="password_user" name="password_user" placeholder="Buat password baru anda" autocomplete="off" required>
                                </div>
                                <div class="input_box">
                                  <label for="bidang_id">Pilih Divisi</label>
                                  <select name="bidang_id" class="form-bidang" required>
                                    <option value="">Tidak memilih divisi</option>
                                    <% bidang.forEach(function(bidang) { %>
                                        <option value="<%= bidang.id_bidang %>" <%= bidang.id_bidang === user.bidang_id ? 'selected' : '' %>><%= bidang.nama_bidang %></option>
                                    <% }); %>                                
                                </select>
                                </div>
                                <div class="input_box">
                                  <label for="foto_profil_user">Foto profil</label>
                                  <input style="padding-top: 8px;" type="file" id="foto_profil_user" name="foto_profil_user" placeholder="Unggah file foto anda" autocomplete="off" required>
                                </div>
                            </div>
                            <div class="reg_btn">
                                <input type="submit" value="Register">
                            </div>
                        </form>
                    </div>            
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>


  

<%- include('../partials/footer__') %>
